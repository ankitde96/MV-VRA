# Repository workflow

- Work directly on `main` unless the user explicitly requests another branch.
- Do not create pull requests unless the user explicitly asks for one.
- Do not create branches implicitly as part of a commit or push request.
- Commit or push only when the user explicitly requests it.
