/**
 * Stable local-only vendor portal credentials. Both the seed and the development auth
 * bypass import this single source of truth so a fresh environment cannot drift.
 */
export const DEV_VENDOR_ID = "6a841ef950d13373304dd55f";
export const DEV_VENDOR_EMAIL = "vendor@mv-vra.local";
export const DEV_VENDOR_PASSWORD = "123456";
